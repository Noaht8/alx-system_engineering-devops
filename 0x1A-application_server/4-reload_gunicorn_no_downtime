#!/usr/bin/env bash
# This script restarts gunicorn app
# Get the PID of the Gunicorn master process
MASTER_PID=$(ps auxf | grep '[gunicorn' | awk '{print $2}')

# Send a HUP signal to the master process to gracefully reload the workers
kill -HUP $MASTER_PID
